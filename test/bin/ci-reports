#!/usr/bin/env php
<?php

/*
 * This file is part of the Enumeration package.
 *
 * Copyright © 2011 Erin Millard
 *
 * For the full copyright and license information, please view the LICENSE
 * file that was distributed with this source code.
 */

// path constants
require dirname(__DIR__).DIRECTORY_SEPARATOR.'src'.DIRECTORY_SEPARATOR.'paths.php';

passthru(LQNT_TEST_BIN_DIR.DIRECTORY_SEPARATOR.'install-vendors', $code);
if (0 !== $code)
{
  exit($code);
}

passthru(LQNT_TEST_BIN_DIR.DIRECTORY_SEPARATOR.'reports', $code);
if (0 !== $code)
{
  exit($code);
}
